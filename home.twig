{% extends "layout.twig" %}

{% block content %}

    <header class="bk-header">
        <div class="logo">
            {{widget('logo', 'logo', {"defaultlogo": asset('images/logo.png')})|raw}}
        </div>
        <div class="companyheader wrapper clearfix">
            <div class="header-left">
                {{widget('profile', 'footercolumn1profilePhone', { 'profileType':'phone', 'phone':'profile'})|raw}} 
            </div>
            <div class="header-center">
                {{widget('companyname', 'companyname', {"business": "profile"})|raw}}
            </div>
            <div class="header-right">
                {{widget('profile', 'footercolumn1socialicons', { 'profileType' : 'socialicons' })|raw}}
            </div>
        </div>       
    </header>

    <div class="mainbody">
        <div class="menu-container">
            {{widget('extendednavigation', 'navigationheader', {})|raw}}
        </div>
        <div class="wrapper clearfix">
            <div class="canvas">
                <div class="canvas1">
                    {{zone('main')|raw}}
                    <div class="footer-text">
                        {{widget('content', 'footercolumn2content', {"content":"<p>100% FRESH FOOD</p>"})|raw}}
                    </div>
                </div>
            </div>
        </div>
    </div>

{% endblock %}
